name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 89.116.192.198
          username: u274401241.grupocotuplas.com
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          local-dir: ./
          server-dir: /home/u274401241/domains/grupocotuplas.com/public_html/
          exclude: |
            **/.git*
            **/.git*/**
            .github/**
            README.md
